#pragma once
#include <iostream>
#include "clsUser.h"

extern clsUser currentUser; // Declare the global variable to store the currently logged-in user data, and we will define it in the main.cpp file, and we will set it to empty user object when there is no logged-in user, and we will update it with the logged-in user data when the user logs in successfully.



